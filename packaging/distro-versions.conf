# LinxPad packaging target versions
# Comma-separated — each value produces a separate package build.
# Single-value entries (tarball, appimage, flatpak) ignore this file.

fedora-versions=43,44
opensuse-versions=tumbleweed
debian-versions=13
ubuntu-versions=24.04
